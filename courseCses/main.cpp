#include <bits/stdc++.h>
#define endl "\n"
#define fwd(a,b) for(int a=0;a<b;a++)

//todo beolvas -> iranyitott graf -> dfs a vegeig -> dfs vissza

using namespace std;

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    int n,m,a,b;
    cin >> n >> m;
    fwd(i,m){
        cin >> a >> b;
    }

    cout << "placeholder" << endl;

    return 0;
}
